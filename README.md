# Hilan-Test

סביבת הכנה מקומית למשימת הבית של חילן — Java ו־Angular.

**קובץ המטלה עדיין לא התקבל. אין כאן עדיין אפליקציה שניתן לבנות או להריץ.**

## סביבת העבודה

- תיקייה: `D:\Documents\ChatGPT\Hilan-Test`
- חשבון GitHub שאומת: `riki-m`
- מאגר פרטי: [riki-m/Hilan-Test](https://github.com/riki-m/Hilan-Test), ענף עבודה `main`.
- נבדק בתאריך 2026-09-08: Git `2.54.0.windows.1`, VS Code `1.135.0`, Node.js `24.12.0`, npm `11.6.2`.
- Node.js המותקן טרם נבדק מול דרישות המטלה. אין בכך קביעה שהוא תואם לגרסת Angular שתתקבל.
- Java/JDK, Maven ו־Gradle לא נמצאו ב־PATH; JDK גם לא נמצא בתיקיות ההתקנה הנפוצות שנבדקו. בחירת הגרסאות והתקנתן ממתינות לפרויקט.
- הותקנו ואומתו: Extension Pack for Java `0.31.1` (כולל תמיכת השפה של Red Hat `1.56.0`, ניפוי שגיאות, בדיקות, ניהול פרויקטים, Maven ו־Gradle) ו־Angular Language Service `22.1.0`. הן מופיעות גם ב־`.vscode/extensions.json`.
- הרחבות Maven ו־Gradle בעורך אינן התקנה של כלי הבנייה עצמם. גם התקנת הרחבת Java אינה קביעה של JDK לבניית המטלה.
- חלון VS Code נפתח בתיקייה עם README זה. כללי Git נבדקו מול דוגמאות של סודות, תוצרי בנייה ותלויות; קובצי wrappers וקובצי נעילה נשארו זמינים למעקב.

## עבודה בתיקייה

לפתיחת התיקייה בעורך:

```powershell
code "D:\Documents\ChatGPT\Hilan-Test"
```

לבדיקת השינויים המקומיים:

```powershell
git status
git diff
```

לפני commit יש לבדוק גם את הקבצים שנבחרו להוספה באמצעות `git diff --cached` ולוודא שאין בהם סודות או תוכן שאסור לשתף.

## עם קבלת ה־ZIP

1. בדקו את תוכן הארכיון ואת הוראות המטלה, לרבות מגבלות שימוש ב־AI ושיתוף. ודאו שנתיבי החילוץ נשארים בתוך התיקייה ושאין דריסה של קבצים קיימים.
2. שלבו את הקבצים תוך שמירה על `.git` ומיזוג קובצי הכנה מתנגשים.
3. מפו את המבנה ובדקו קבצים כגון `pom.xml`, `build.gradle`, `build.gradle.kts`, `package.json`, קובצי נעילה, `.nvmrc`, `.java-version` ו־wrappers, ככל שקיימים.
4. התאימו את גרסאות הכלים לפי הראיות בפרויקט. אל תשדרגו תלויות כחלק מההכנה ללא צורך.
5. בדקו סודות, שמרו commit של מצב המקור לפני תיקונים, ובצעו ניסיון בנייה ובדיקות כדי לתעד את נקודת ההתחלה.
6. השלימו תיקונים ממוקדים ואמתו אותם. עדכנו כאן את פקודות ההרצה והתוצאות המאומתות.

אין להעלות את ה־ZIP המקורי למאגר. אין להעלות את קוד המטלה לפני בדיקת הוראות השיתוף והסודות.
