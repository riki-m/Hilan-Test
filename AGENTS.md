# הנחיות עבודה — מבחן חילן

- תיקיית העבודה היא `D:\Documents\ChatGPT\Hilan-Test`. בדקו את מצב Git ואת הקבצים לפני שינוי.
- לפני שינוי בקוד, קראו את הוראות המטלה ואת ההנחיות הנוספות בפרויקט. כבדו מגבלות מפורשות על שימוש ב־AI ועל שיתוף הקוד.
- לפני חילוץ ZIP בדקו את רשימת הקבצים, נתיבי החילוץ וההתנגשויות עם קבצים קיימים. אל תדרסו את `.git` או את קובצי ההכנה; מזגו הנחיות לפי הצורך.
- זהו גרסאות Java, Node.js, Angular וכלי בנייה מתוך קובצי הפרויקט. אל תניחו שימוש ב־Spring Boot. העדיפו wrappers קיימים וכלים מקומיים לפרויקט.
- שמרו נקודת התחלה ב־Git לפני תיקונים, לאחר בדיקת סודות והוראות המטלה. אין להעלות קוד מטלה לפני בדיקת מגבלות שיתוף, גם כשהמאגר פרטי.
- שמרו על המבנה והסגנון הקיימים. בצעו תיקונים ממוקדים; אל תוסיפו תלויות או תשנו ארכיטקטורה ללא צורך מבוסס.
- אין לשמור סודות, פרטי התחברות, תלויות מותקנות או תוצרי בנייה ב־Git. בדקו את התוכן המיועד ל־commit; `.gitignore` אינו מזהה סודות בתוך קובצי קוד.
- בצעו בנייה, בדיקות והרצה בהתאם לפרויקט ולשינוי. דווחו במדויק מה אומת ומה חסום; אל תמציאו פקודות הרצה או תוצאות בדיקה.
- הסבירו בעברית בקצרה את הבעיה, התיקון והאימות, באופן שיאפשר למועמד להסביר אותם בראיון.
