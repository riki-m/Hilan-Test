# דוח מעקב QA — מטלת חילן

> עדכון שלב ההכנה, 2026-09-09: הדוח המקורי להלן מתאר בדיקה מוקדמת וכעת אינו מצב הפרויקט הנוכחי. קוד המטלה נמצא בתיקייה ונשמר ב-commit `455571f`. בניית Java ו-Angular עברה, בדיקת Angular עברה (1/1), ושרת Angular החזיר HTTP 200. בדיקת השרת והרצת PostgreSQL/API חסומות משום ש-Docker/WSL אינם מוכנים וכונן C כמעט מלא. תוצאות מפורטות והמשך עבודה נמצאים ב-[דוח הרצת המקור](docs/BASELINE_RUN.md). לא בוצעו תיקוני קוד. תוכן הבדיקה המוקדמת נשמר להלן כהיסטוריה בלבד.

תאריך בדיקה: 2026-09-09

## מצב נוכחי

**בדיקת המטלה חסומה: קוד האפליקציה והוראות המטלה אינם בתיקיית העבודה.** אין להסיק מכך שהיישום תקין או שהמטלה הושלמה.

נבדקה התיקייה `D:\Documents\ChatGPT\Hilan-Test`. נקודת הייחוס שנצפתה: ענף `main`, commit ‏`410959f` (`Prepare private Java and Angular assignment workspace`). מצב Git היה נקי לפני יצירת דוח זה. לא בוצע אימות מול המאגר המרוחק.

## ראיות מהבדיקה הנוכחית

- נקראו `AGENTS.md` ו־`README.md`.
- נבדקו רשימת הקבצים בתיקייה, כולל קבצים מוסתרים, ורשימת הקבצים שבמעקב Git.
- קיימים קובצי הכנה בלבד: `.gitignore`, `.vscode/extensions.json`, `AGENTS.md`, `README.md`, וכן תיקיית `.git`.
- לא נמצאו קוד יישום, ZIP, מסמך מטלה, קובצי בנייה או בדיקות.
- ה־README מציין במפורש שקובץ המטלה טרם התקבל ושאין אפליקציה לבנייה ולהרצה.
- גרסאות הכלים ותוצאות ההכנה המופיעות ב־README הן תיעוד קודם; הן לא אומתו מחדש בבדיקה זו ואינן ראיה לתאימות לפרויקט שיתקבל.

## מעקב בדיקות

| תחום | תנאי הצלחה ותרחיש | מצב | ראיה / תוצאה בפועל | הפעולה הבאה |
| --- | --- | --- | --- | --- |
| זיהוי סביבת העבודה | התיקייה היא מאגר Git וניתן לזהות את מצב המקור | עבר | נבדקו המיקום, מצב Git, היסטוריית commit ורשימת קבצים | לבדוק שוב לפני שינוי בקוד |
| קריאת הנחיות מקומיות | קריאת כל ההנחיות הזמינות לפני בדיקה ושינוי | עבר | נקראו AGENTS.md ו־README.md | לקרוא גם הנחיות שיגיעו עם המטלה |
| מיפוי דרישות ומגבלות AI ושיתוף | חילוץ דרישות וקביעת תנאי קבלה מתוך מסמך המטלה | חסום | מסמך המטלה חסר | לקבל את ההוראות המקוריות |
| זיהוי גרסאות וכלי בנייה | לזהות גרסאות מתוך הגדרות הפרויקט ו־wrappers | חסום | אין קובצי פרויקט | לבדוק לאחר קבלת הקוד |
| בנייה ובדיקות קיימות | לבצע את תהליך הבנייה והבדיקות שהפרויקט מגדיר | חסום | אין אפליקציה או בדיקות להרצה | להריץ ולתעד תוצאות לאחר קליטת הפרויקט |
| תהליכי משתמש מלאים | לאמת תוצאה דרך הממשק, השרת והאחסון ככל שקיימים | חסום | אין יישום ואין דרישות לתרחישים | לגזור תרחישים מהמטלה ולהריץ |
| קלט שגוי, מקרי קצה ושגיאות | התנהגות תואמת לדרישות ללא פגיעה בנתונים | חסום | אין מימוש לבדיקה | להגדיר תרחישים בהתאם לקוד ולדרישות |
| התמדה ורגרסיות | לאמת שמירה והשפעות שינויים בהתאם לדרישות | חסום | אין מימוש או תיקונים לבדיקה | לקבוע בדיקות לפי המימוש והשינויים |
| מוכנות להגשה | כל דרישה מחייבת אומתה וכל תקלה חוסמת נסגרה | חסום | טרם ניתן למפות את דרישות המטלה | להשלים מיפוי ובדיקות לפני מסקנת הגשה |

## ממצאים פתוחים

חסם קליטה: חסרים קוד המטלה וההוראות המקוריות. זהו חסם לבדיקה, ולא תקלה שהוכחה באפליקציה. לא בוצעו בנייה, בדיקות אוטומטיות, בדיקות API, בדיקות ממשק או בדיקות מסד נתונים. לא בוצעו תיקוני קוד.

## הפעולה הנדרשת מהמשתמשת

להניח בתיקיית העבודה את ה־ZIP המקורי ואת מסמך ההוראות, או למסור את הנתיב המדויק שלהם אם כבר נשמרו במחשב. אין צורך לחלץ את ה־ZIP מראש.

## סדר המשך העבודה

1. לקרוא את הוראות המטלה ולבדוק מגבלות AI ושיתוף.
2. לבדוק את רשימת קובצי ה־ZIP, בטיחות נתיבי החילוץ והתנגשויות לפני חילוץ; לשמור על `.git` וקובצי ההכנה.
3. למפות דרישות למימוש ולתרחישי בדיקה, ולזהות גרסאות וכלים מתוך הפרויקט.
4. לבדוק סודות והוראות ולשמור נקודת מקור ב־Git לפני תיקונים.
5. להריץ את הבדיקות הקיימות ולבדוק את ההתנהגות לפני כל תיקון; להרחיב לבדיקות חסרות לפי סיכון ודרישות.
6. לתעד תקלות עם שלבי שחזור, תוצאה צפויה, תוצאה בפועל וחומרה; לא לסגור ללא אימות חוזר.

בכל המשך עבודה יש להשוות את מצב הקוד לנקודת הייחוס של הבדיקות. תוצאות קודמות שהושפעו משינוי מחייבות אימות מחדש. יש לשמור הפרדה בין קריאת קוד לבין ראיות מהרצה בפועל.
