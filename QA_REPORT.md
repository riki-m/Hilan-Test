# דוח QA — מטלת חילן

## עדכון B1 — 2026-09-09, לאחר חזרת הסביבה

דרישת B1 אומתה: 37 בדיקות עברו (20 יחידה, 4 MockMvc, 13 PostgreSQL), ושמונה תרחישי HTTP נבדקו מול המסד. תצוגת הרשומות אומתה בדפדפן לאחר רענון. אין טופס יצירה ולכן אין טענה לאימות יצירה דרך המסך. אין כשל יישום חדש; נוספו בדיקות מסד בלבד. Docker זמין לבדיקות B1 עם התאמת API לתהליך הבדיקה. פרטים, ראיות וגבולות האימות ב-[B1_QA](docs/B1_QA.md).

B2 אינו מקבל סטטוס חדש בסבב זה. DECISIONS לא נערך; נדרש להעביר לבעל המסמך סיכום B1 ופקודת ההרצה. אין push.

## היסטוריית סבב B2 — לפני אימות B1 הנוכחי

2026-09-09: **B1 ו-B2 מומשו ואומתו חלקית; אין אימות מערכת מלאה.**

הרצה ב-15:51:44: 20 בדיקות יחידה ו-4 MockMvc של B1, ו-23 בדיקות יחידה ו-4 MockMvc של B2 — סה״כ 51, ללא כישלונות/שגיאות/דילוגים; BUILD SUCCESS. קוד השרת וכל מקורות הבדיקות קומפלו. אין שרת רשת או מסד בהרצה זו.

ב-B2 נוסף נתיב אישור עם שירות טרנזקציוני, נעילה לפי עובד וחישוב מכסה משותף. בדיקות B1 הורצו מחדש לאחר השינוי ועברו. הוכנו חמש בדיקות PostgreSQL נוספות, כולל מקביליות ו-rollback; הן קומפלו בלבד. אין להשתמש בבדיקות מדומות כהוכחה ליעילות נעילות או לביטול שינוי במסד.

רשימות התרחישים והראיות מנוהלות בנפרד: [B1_QA](docs/B1_QA.md), [B2_QA](docs/B2_QA.md). ראיה עדכנית וחתימות קוד: [B2-test-run.json](docs/evidence/B2-test-run.json). החלטות ומגבלות: [DECISIONS](DECISIONS.md). סדר המשך: [TASK_TRACKER](docs/TASK_TRACKER.md).

Docker/WSL היו חסומים בניסיון ההפעלה האחרון; בעקבות זאת המשתמשת עצרה את הטיפול בסביבה. לא הופעלו או נבדקו שוב בסבב B2. אימות PostgreSQL, HTTP ברשת ומסך נשאר פתוח. אין פעולה אישית נדרשת כעת; לפני B3 יש לחזור לאימות מסד משותף לאחר אישור וסביבה זמינה.
