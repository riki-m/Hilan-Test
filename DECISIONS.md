# DECISIONS — מצב B1 ו-B2

## 1. החלטות ארכיטקטוניות

תיקון ממוקד ב-`backend/src/main/java/com/example/leavemanagement/controller/LeaveRequestsController.java`, במתודה `create`. במהלך B2 חישוב השנה הועבר ללא שינוי מדיניות ל-`service/VacationBalancePolicy.java`, המשמש יצירה ואישור. ללא תלויות חדשות או שינוי מבנה. ב-B2 נוספה הפרדה מינימלית לשירות אישור עבור גבול טרנזקציה; שיפור השכבות הכללי B3 עדיין לא בוצע. בדיקות יחידה משתמשות ב-Mockito; בדיקות MockMvc בודקות מיפוי ו-JSON בתוך התהליך, ללא שרת רשת. בדיקות PostgreSQL של B1 הורצו ועברו; פרטי B1 בסעיף 2. מצב B2 נשאר מתועד בנפרד בסעיף 3.

## 2. הבאג ביתרת החופשה

במקור (`455571f`) חושב הניצול המאושר אך התנאי בדק רק את הבקשה החדשה מול המכסה המלאה. לדוגמה: מכסה 20, ניצול 18, בקשה 3 — התקבלה בטעות. כעת נבדק סכום הניצול המאושר והימים המבוקשים בכל שנה מול המכסה. חריגה מחזירה 400 לפני שמירה; בדיוק היתרה מותרת ונשמרת במצב PENDING.

מדיניות עסקית מפורשת: שנה קלנדרית, ללא העברת יתרות; בקשה חוצת שנה מחולקת לפי חפיפת התאריכים לכל שנה. זו הנחת מימוש שלנו, משום שהמטלה אומרת מכסה שנתית אך אינה מגדירה את השיוך. המכסה הנוכחית של העובד חלה בכל שנה; המודל אינו מכיל היסטוריית שינויי מכסה. ימים קלנדריים כוללים את שני קצות הטווח, בהתאם להערה במודל. הסכימה משתמשת ב-long, ומספר הימים הנשמר חייב להתאים ל-int.

רק VACATION במצב APPROVED של העובד נחשב ניצול. PENDING ו-REJECTED אינם שומרים מכסה. מחלה וחל״ת אינן מוגבלות במכסת חופשה. חופשות היסטוריות מחושבות מהתאריכים, ולא משדה days שעלול להיות לא עקבי. לא מבוצע תיקון אוטומטי של היסטוריה פגומה או איחוד טווחים חופפים.

נוספה בדיקת שדות חובה וסדר תאריכים בשרת כדי למנוע שמירת בקשות לא תקינות ועקיפת מכסה באמצעות ימים שליליים. אין שינוי ב-Angular.

האימות ב-2026-09-09 הסתיים ב-16:28:06: 37 בדיקות עברו — 20 יחידה, 4 MockMvc ו-13 PostgreSQL ב-Testcontainers; אפס כישלונות, שגיאות ודילוגים. רגרסיית `create_ExceedingRemainingQuota_IsRejectedWithoutSaving` עברה מול מסד אמיתי: מכסה 20, ניצול מאושר 18, בקשת 3 נדחתה ב-400 ללא תוספת רשומה. נוספו עשרה מקרי מסד; לא נדרש תיקון נוסף בקוד היישום.

פירוט התרחישים וראיות ההרצה ב-[B1_QA](docs/B1_QA.md), כולל גבולות מכסה, הפרדת עובדים וסוגים, חציית שנים והתמדה. [ראיית JUnit](docs/evidence/B1-postgres-run.json) כוללת שמות בדיקות וחתימות מקורות.

בנוסף עברו שמונה תרחישי HTTP עם בדיקת SQL עצמאית. בממשק נבדקה תצוגת התוצאות לאחר רענון; אין טופס יצירה בשלד ולכן לא נטען לאימות יצירה דרך המסך. ראיות: [HTTP](docs/evidence/B1-live-http.json), [ממשק](docs/evidence/B1-ui-run.json).

רגרסיית B1 בסבב B2: לאחר ניצול 16 אושרה בקשה בת יומיים, והניצול הפך ל-18. אישור חוזר נדחה; יצירת 3 ימים נדחתה ו-2 התקבלה. עובד אחר עם ניצול 20 לא השפיע. תרחיש המסד `approvalUpdatesCreationBalanceWithoutDoubleChargeOrCrossEmployeeMixing` עבר, לצד כל 37 בדיקות B1, ללא שינוי נוסף בקוד היישום.

## 3. אישור בקשה (approve) ו-concurrency

B2 מומש ב-`service/LeaveApprovalService.java`, ללא שינוי סכימה או תלויות. Controller קורא לשירות דרך Spring, כך שהטרנזקציה עוטפת את כל הפעולה; קוד HTTP ממופה רק מחוץ לגבול הטרנזקציה. הצלחה מחזירה 200 עם הבקשה ו-status=1. בקשה חסרה מחזירה 404; סטטוס לא ממתין, חוסר יתרה, בעלות שהשתנתה או נתוני בקשה פגומים מחזירים 409 וטקסט ברור. אלה קודי התכנון שלנו, לא מספרים שנקבעו במטלה. כשל מסד אינו נתפס כשגיאה עסקית ואינו מוחזר כהצלחה.

מנגנון יחיד: `PESSIMISTIC_WRITE` על רשומת העובד, בתוך טרנזקציית `READ_COMMITTED`. תחילה נשלף מזהה העובד כערך בלבד (ללא טעינת Entity), ואז ננעל העובד. רק לאחר קבלת הנעילה נטענים הבקשה ומצב הניצול העדכניים. כך שתי בקשות שונות של אותו עובד מתחרות על אותה נעילה, והמאשרת השנייה רואה את תוצאת הראשונה לאחר commit. שני ניסיונות לאותה בקשה נבדקים באותו סדר; השני יידחה לאחר שינוי הסטטוס. אין נעילה גלובלית לכל העובדים.

כל הבדיקות העסקיות קודמות לשינוי הסטטוס. לאחריהן מתבצע APPROVED ו-saveAndFlush; ה-commit מתרחש ביציאה דרך Spring. חריגה בזמן flush/commit מבטלת את הטרנזקציה, ואין להציג שינוי באובייקט בזיכרון כהוכחת rollback במסד. טסט PostgreSQL נפרד הוכן להוכיח rollback אחרי flush. אין קוד שמתקן בשקט days לא עקבי בבקשה המועמדת לאישור: הוא נדחה כ-409.

גבולות: כל מסלול שמאשר חייב להשתמש באותה נעילת עובד. עדכון SQL חיצוני העוקף אותה אינו מוגן. אין כרגע מסלול להעברת בקשה לעובד אחר; נבדקת גם התאמת בעלות לאחר הנעילה. אין שמירת מכסה ל-PENDING, שינוי מדיניות שנים, מנגנון retries אוטומטי או timeout ייעודי לנעילות בייצור. המתנה/כשל תשתיתי אינם הוכחה לחריגה עסקית. בדיקות מקביליות מוגבלות בזמן הוכנו אך לא הורצו. **התנהגות נעילות וטרנזקציות ב-PostgreSQL לא אומתה.**

תרחישים וראיות B2 ב-[B2_QA](docs/B2_QA.md). אישור יחיד נבדק בלוגיקה וב-MockMvc בלבד; מסד, HTTP ברשת ומסך חסומים. אין הכרזה על B2 כשלם מקצה לקצה.

## 4. על מה ויתרנו בגלל הזמן והמגבלות

- חובה ב-B1: תיקון הבאג וטסט רגרסיה. בדיקות היחידה מוכיחות את החישוב; רגרסיית PostgreSQL ממתינה לאימות.
- קשור ישירות לתיקון: גבולות מכסה, שנה, קלט שגוי, אי-שמירה וחוזה JSON. נבדק בהיקף המתועד בדוח.
- חסם סביבתי: ניסיון הפעלת Docker/WSL האחרון נכשל בתשתית הווירטואליזציה של Windows. בעקבותיו המשתמשת עצרה את ניסיונות ההפעלה. PostgreSQL, HTTP ברשת והתהליך במסך לא אומתו; אין הכרזה על סגירת B1 מקצה לקצה. פירוט החסם והערכת המוכנות ב-docs/B1_QA.md.
- נדחה: אימות טרנזקציות האישור מול מסד, הפרדת שכבות כללית ל-B3, תיקוני מסך ל-F1–F3. בדיקת מסך לפני B2 אינה דרישה מפורשת של README.
- עם עוד זמן: אימות מסד וממשק תחילה; אחריו מדיניות היסטוריית מכסות, נתונים פגומים וטווחים חופפים. השאילתה עדיין טוענת את כל החופשות המאושרות של העובד, והחישוב עובר על שנות הבקשה; אופטימיזציה להגשות חריגות בהיקפן נדחתה במסגרת ה-POC.

## 5. שימוש ב-AI

### דוגמאות אמיתיות מהשיחה (הציטוטים הם קטעים מדויקים מה-prompts)

1. Prompt: "אני רוצה שלא נפספס שום דבר אני רוצה שתכתוב לי מה הבעיה שיש בבאג מה תוקן? ומה אתה ממליץ לבדוק , אל תשנה שום דבר לפני שאתה מבקש את האישור שלי. הבדיקה וההשואה הוא ל B1"
   תוצאה: השוואה למקור, הסבר 18+3 מול 20, זיהוי פערי שנים והפרדה בין בדיקות יחידה למסד, ללא שינוי באותו שלב.
2. Prompt: "אז אני הולכת לפי ההמלצה שלך ומאשרת לך כרגע לבצע בדיקות וכן אם מצאת באג מאשרת לתקן את הקוד"
   תוצאה: הרחבת בדיקות חשפה 5 כישלונות מתוך 16; תיקון שיוך לשנה ותאריכים הפוכים, והרחבה ל-20 בדיקות שעברו. ראיות היסטוריות בדוח B1.
3. Prompt: "בבדיקה האחרונה נמצאה הרצה של 20 בדיקות יחידה שעברו, בעוד שחלק מהמסמכים עדיין מתארים שש בדיקות. אמת מה נכון כעת וסנכרן את התיעוד עם המימוש בפועל."
   תוצאה: בדיקת יומנים, הרצה חוזרת, ריכוז מטריצת QA, הוספת בדיקות MockMvc והכנת בדיקת סינון מסד ללא הפעלת Docker.

### הצעת AI שתוקנה בפועל

בסבב הנוכחי AI כתב בבדיקת JSON ציפייה ל-`"PENDING"`. ההרצה נכשלה: בפועל התקבל `0`. בדיקת `LeaveStatus.java` הראתה שהמודל מגדיר במפורש סריאליזציה מספרית, התואמת ללקוח Angular. תוקנה ציפיית הבדיקה ל-0 וגם קלט סוג החופשה הותאם לקוד המספרי הקיים. לא שינינו את חוזה ה-API כדי להתאים לבדיקה שגויה. ראיה: `.local-run/b1-audit.log`; האימות החוזר ב-`.local-run/b1-audit-final.log`. זה תיקון אמיתי של הצעת AI, לא תקלה שהתגלתה ביישום.

### אבטחה

ב-`LeaveRequestsController.search` נבנה SQL בשרשור קלט שם. הסיכון הוא SQL injection; הטיפול נשאר לבונוס S1 ולא בוצע כחלק מ-B1. אין טענה שהפרויקט עבר ביקורת אבטחה מלאה. אין צורך ב-GitHub Discussions: מסמך זה הוא תוצר ההגשה הנדרש.

### המשך תיעוד AI ב-B2

התכנון שביקשה המשתמשת כלל: "אל תסתפק באמירה ‘נוסיף @Transactional’: הסבר כיצד המנגנון מגן על הכלל העסקי ב־PostgreSQL" (ציטוט עם התאמת סימני מירכאות). לאחר אישור "מאשרת" נכתב שירות אישור עם נעילת עובד וקריאת scalar לפני הנעילה, וכן בדיקות מבודדות ובדיקות מסד חסומות. זו הרחבה לדוגמת השימוש בתכנון ובבדיקות, ולא טענה שהמקביליות הוכחה בהרצה.

## 6. הוראות הרצה

README המקורי נשאר ללא שינוי. בסביבה המקומית קיימים JDK 21 ו-Maven. להרצה ללא Docker, משורש התיקייה ב-PowerShell:

```powershell
. .\scripts\Use-LocalTools.ps1
Set-Location backend
mvn.cmd -o '-Dmaven.repo.local=../.local-tools/m2' '-Dtest=LeaveBalanceTests,LeaveRequestHttpTests,LeaveApprovalTests,LeaveApprovalHttpTests' test
```

פקודה זו מפעילה רק בדיקות יחידה ו-MockMvc, ללא שרת רשת או מסד. היא מקמפלת גם את מקורות בדיקות PostgreSQL אך אינה מריצה אותן. במחשב אחר עם תלויות זמינות אפשר להריץ מתוך backend: `mvn -Dtest=LeaveBalanceTests,LeaveRequestHttpTests,LeaveApprovalTests,LeaveApprovalHttpTests test`.

רק לאחר שינוי ההנחיה והכנת סביבה: `mvn test` מתוך backend מפעיל גם את Testcontainers ודורש Docker פעיל. פקודה זו לא הורצה בסבב הנוכחי. ההגדרות המקומיות אינן מחייבות מחשב של בודק אחר.
