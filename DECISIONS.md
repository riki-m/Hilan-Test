# DECISIONS — מצב B1, B2, B3 ו-F1

עדכון F1: טופס Angular מומש ונבדק בבדיקות עם HTTP מדומה; בנייה ו-13 בדיקות עברו. טרם אומתה הגשה מהטופס דרך השרת ושמירה במסד. האמירות בהמשך על היעדר שינוי ב-Angular מתייחסות לסבבי B1–B3 בלבד. פירוט: [F1_QA](docs/F1_QA.md).

עדכון לפי הרצות 2026-09-09: **חסם Docker/WSL הוסר בהיקף B1 שנבדק. B1 הורץ בהצלחה מול PostgreSQL אמיתי, HTTP ברשת והממשק הקיים.** עדכון B2 המאוחר: 83 בדיקות שרת עברו ב-16:55:19, כולל 19 בדיקות PostgreSQL של B2 ו-37 רגרסיות B1. ראיות HTTP וממשק נשמרו גם הן; פירוט ב-B2_QA.

## 1. החלטות ארכיטקטוניות

לאחר B3, LeaveRequestsController מטפל בקבלת HTTP/JSON, קריאה לשירותים ומיפוי תשובות בלבד. היצירה, בדיקת שדות/טווח וכללי המכסה הועברו ל-LeaveRequestService; הרשימה והחיפוש עוברים דרך שירות זה ל-LeaveRequestRepository, שבו מתבצעים המיון והשאילתה. השירות החדש מחזיר Entity או כשל CreationException מסווג, וה-Controller שומר על קודי 400/404 ועל טקסטי השגיאה הקיימים.

LeaveApprovalService ומדיניות VacationBalancePolicy שכבר הופרדו ב-B2 נשמרו, ללא שכבה כפולה. יצירה היא טרנזקציונית, קריאות הרשימה והחיפוש readOnly; אישור נשאר בשירות ובטרנזקציה המקוריים. EmployeesController נשאר lookup פשוט ישירות למאגר משום שאין בו תהליך עסקי או תיאום בין מאגרים המצדיק שירות נוסף.

לא נוספו תלויות, סכימה, ממשקי שירות ריקים, שכבה גנרית או mapper. תקינות קלט נבדקת בשירות לפני גישה למסד גם עבור קורא שאינו HTTP; parsing של טיפוסים ו-JSON נשאר ב-Spring MVC. חוזה חריגות האישור הקיים נשמר כדי להימנע משכתוב שאינו נחוץ. אין שינוי ב-Angular או במדיניות המכסה.

### F1 — טופס יצירה ב-Angular

נבחר Reactive Forms מתוך התלויות הקיימות: טופס עם מזהה עובד, סוג חופשה מספרי ותאריכי התחלה וסיום, ללא שינוי בחוזה השרת. נוספו בדיקות שדות חובה, בחירות תקינות ותאריכים, וחישוב ימים קלנדריים כולל הקצוות באמצעות UTC כדי להימנע מהפרשי שעון קיץ. יתרת החופשה עצמה נאכפת בשרת; הטופס מציג הודעה כאשר השרת דוחה חריגה.

שליחה כפולה נחסמת בזמן בקשה. בכישלון הקלט נשמר לניסיון נוסף; בהצלחה הטופס מתאפס והבקשה מתווספת לרשימה בלי GET נוסף. שם העובד מושלם מרשימת הבחירה כשהתגובה אינה מכילה employee. כשל בטעינת רשימת הבקשות משחרר את מצב הטעינה כדי שלא לחסום את טופס היצירה לצמיתות. התנהגות כפתור האישור לא שונתה; F2 ו-F3 נותרו להמשך. לא נוספו תלויות ולא שונו קובצי שרת.

בניית Angular עברה, וכן 13 בדיקות: 12 חדשות ל-F1 ובדיקת האפליקציה הקיימת. הבדיקות משתמשות ב-HttpTestingController ואינן הוכחה להתמדה במסד. ראיות ההרצה, התרחישים וההמשך מתועדים ב-[F1_QA](docs/F1_QA.md).

## 2. הבאג ביתרת החופשה


במקור (`455571f`) חושב הניצול המאושר אך התנאי בדק רק את הבקשה החדשה מול המכסה המלאה. לדוגמה: מכסה 20, ניצול 18, בקשה 3 — התקבלה בטעות. כעת נבדק סכום הניצול המאושר והימים המבוקשים בכל שנה מול המכסה. חריגה מחזירה 400 לפני שמירה; בדיוק היתרה מותרת ונשמרת במצב PENDING.

מדיניות עסקית מפורשת: שנה קלנדרית, ללא העברת יתרות; בקשה חוצת שנה מחולקת לפי חפיפת התאריכים לכל שנה. זו הנחת מימוש שלנו, משום שהמטלה אומרת מכסה שנתית אך אינה מגדירה את השיוך. המכסה הנוכחית של העובד חלה בכל שנה; המודל אינו מכיל היסטוריית שינויי מכסה. ימים קלנדריים כוללים את שני קצות הטווח, בהתאם להערה במודל. הסכימה משתמשת ב-long, ומספר הימים הנשמר חייב להתאים ל-int.

רק VACATION במצב APPROVED של העובד נחשב ניצול. PENDING ו-REJECTED אינם שומרים מכסה. מחלה וחל״ת אינן מוגבלות במכסת חופשה. חופשות היסטוריות מחושבות מהתאריכים, ולא משדה days שעלול להיות לא עקבי. לא מבוצע תיקון אוטומטי של היסטוריה פגומה או איחוד טווחים חופפים.

נוספה בדיקת שדות חובה וסדר תאריכים בשרת כדי למנוע שמירת בקשות לא תקינות ועקיפת מכסה באמצעות ימים שליליים. אין שינוי ב-Angular.

האימות ב-2026-09-09 הסתיים ב-16:28:06: 37 בדיקות עברו — 20 יחידה, 4 MockMvc ו-13 PostgreSQL ב-Testcontainers; אפס כישלונות, שגיאות ודילוגים. רגרסיית `create_ExceedingRemainingQuota_IsRejectedWithoutSaving` עברה מול מסד אמיתי: מכסה 20, ניצול מאושר 18, בקשת 3 נדחתה ב-400 ללא תוספת רשומה. נוספו עשרה מקרי מסד; לא נדרש תיקון נוסף בקוד היישום.

פירוט התרחישים וראיות ההרצה ב-[B1_QA](docs/B1_QA.md), כולל גבולות מכסה, הפרדת עובדים וסוגים, חציית שנים והתמדה. [ראיית JUnit](docs/evidence/B1-postgres-run.json) כוללת שמות בדיקות וחתימות מקורות.

בנוסף עברו שמונה תרחישי HTTP עם בדיקת SQL עצמאית. בממשק נבדקה תצוגת התוצאות לאחר רענון; אין טופס יצירה בשלד ולכן לא נטען לאימות יצירה דרך המסך. ראיות: [HTTP](docs/evidence/B1-live-http.json), [ממשק](docs/evidence/B1-ui-run.json).

רגרסיית B1 בסבב B2: לאחר ניצול 16 אושרה בקשה בת יומיים, והניצול הפך ל-18. אישור חוזר נדחה; יצירת 3 ימים נדחתה ו-2 התקבלה. עובד אחר עם ניצול 20 לא השפיע. תרחיש המסד `approvalUpdatesCreationBalanceWithoutDoubleChargeOrCrossEmployeeMixing` עבר, לצד כל 37 בדיקות B1, ללא שינוי נוסף בקוד היישום.

## 3. אישור בקשה (approve) ו-concurrency

B2 מומש ב-`service/LeaveApprovalService.java`, ללא שינוי סכימה או תלויות. Controller קורא לשירות דרך Spring, כך שהטרנזקציה עוטפת את כל הפעולה; קוד HTTP ממופה רק מחוץ לגבול הטרנזקציה. הצלחה מחזירה 200 עם הבקשה ו-status=1. בקשה חסרה מחזירה 404; סטטוס לא ממתין, חוסר יתרה, בעלות שהשתנתה או נתוני בקשה פגומים מחזירים 409 וטקסט ברור. אלה קודי התכנון שלנו, לא מספרים שנקבעו במטלה. כשל מסד אינו נתפס כשגיאה עסקית ואינו מוחזר כהצלחה.

מנגנון יחיד: `PESSIMISTIC_WRITE` על רשומת העובד, בתוך טרנזקציית `READ_COMMITTED`. תחילה נשלף מזהה העובד כערך בלבד (ללא טעינת Entity), ואז ננעל העובד. רק לאחר קבלת הנעילה נטענים הבקשה ומצב הניצול העדכניים. כך שתי בקשות שונות של אותו עובד מתחרות על אותה נעילה, והמאשרת השנייה רואה את תוצאת הראשונה לאחר commit. שני ניסיונות לאותה בקשה נבדקים באותו סדר; השני יידחה לאחר שינוי הסטטוס. אין נעילה גלובלית לכל העובדים.

כל הבדיקות העסקיות קודמות לשינוי הסטטוס. לאחריהן מתבצע APPROVED ו-saveAndFlush; ה-commit מתרחש ביציאה דרך Spring. חריגה בזמן flush/commit מבטלת את הטרנזקציה, ואין להציג שינוי באובייקט בזיכרון כהוכחת rollback במסד. טסט PostgreSQL נפרד עבר והוכיח rollback אחרי flush בעקבות חריגה מכוונת בטרנזקציה חיצונית; לא נבדק כל סוג אפשרי של כשל commit. אין קוד שמתקן בשקט days לא עקבי בבקשה המועמדת לאישור: הוא נדחה כ-409.

גבולות: כל מסלול שמאשר חייב להשתמש באותה נעילת עובד. עדכון SQL חיצוני העוקף אותה אינו מוגן. אין כרגע מסלול להעברת בקשה לעובד אחר; נבדקת גם התאמת בעלות לאחר הנעילה. אין שמירת מכסה ל-PENDING, שינוי מדיניות שנים, מנגנון retries אוטומטי או timeout ייעודי לנעילות בייצור. המתנה/כשל תשתיתי אינם הוכחה לחריגה עסקית. בדיקות המקביליות וה-rollback עברו מול PostgreSQL בסבב B2; הכיסוי אינו הוכחה לכל כשל commit אפשרי.

תרחישים וראיות B2 ב-[B2_QA](docs/B2_QA.md). בסבב המאוחר עברו 23 בדיקות יחידה, 4 MockMvc ו-19 PostgreSQL של B2, לצד 37 רגרסיות B1. נשמרו ראיות ל-11 תרחישי HTTP עם בדיקת מסד ולאישור מהמסך. ראיות: [הרצת B2](docs/evidence/B2-postgres-run.json), [HTTP וממשק](docs/evidence/B2-live-http.json). הרחבת בדיקות B2 והראיות נשמרו בקומיט 0ddf6a2 והועלו לענף codex/b2-verified.

### נתוני אימות B2 ותוצאות

- יתרה 2 (מכסה 20 וניצול 18), ושתי בקשות שונות של 2 ימים: הצלחה אחת ו-409 אחד; סך הניצול 20 ובקשה אחת נשארה PENDING.
- שני ניסיונות לאותה בקשה: הצלחה אחת ו-409 אחד, ורשומה מאושרת אחת.
- ניצול 16 ושתי בקשות של 2: שתי הצלחות וסך ניצול 20.
- שני עובדים עם ניצול 18 ובקשה של 2 לכל אחד: השני השלים אישור בזמן שהראשון המתין לנעילה; שניהם הסתיימו ב-20 ללא ערבוב בעלות.

הבדיקות השתמשו בטרנזקציות נפרדות, נתוני הכנה שנשמרו לפני התחלת הפעולות וחיווי `pg_blocking_pids` להוכחת המתנה אמיתית, עם זמני המתנה מוגבלים. עצם הפעלת threads או הצלחת mocks אינה הראיה למקביליות.

ב-HTTP נבדקו גם `abc` ומספר החורג מטווח long → 400; מזהה ‎-1 או מזהה חיובי שאינו קיים → 404. במקרה 404 הגוף היה `Leave request not found`; במקרה מצב סופי 409 הגוף היה `Only pending requests can be approved`; חריגת מכסה החזירה 409 עם `Not enough vacation balance`. אלו בחירות חוזה, לא דרישות לקוד מספרי מסוים במטלה.

נתונים לדוגמה: עובד 4, ניצול 10+8; בקשה 10 בת 3 ימים נדחתה, בקשה 11 בת 2 אושרה, אישור חוזר שלה נדחה, ובקשה 12 בת יום נדחתה לאחר מיצוי המכסה. בקשה דחויה 13 נשארה REJECTED. מחלה 14 וחל״ת 15 אושרו גם עם ניצול חופשה 20. תמונת המסד לפני ואחרי הוכיחה שרק הסטטוס המותר השתנה. בקשה 17 של עובד 5 אושרה מהמסך, ונצפתה APPROVED לאחר רענון, GET ו-SQL. הנתונים והתגובות המלאות נשארים בדוח B2 ובקובץ הראיה המקושר, ללא רשימת מעקב נוספת כאן.

עדכון B3: כל 19 בדיקות PostgreSQL של B2 עברו שוב לאחר הפרדת השכבות, כולל חפיפה, אותה בקשה, בקשות שונות, עובדים שונים ו-rollback. שירות האישור וגבול READ_COMMITTED/נעילת העובד לא השתנו; הקריאה עדיין עוברת דרך Spring proxy. טרנזקציה נוספה ליצירה, ובדיקה מול מסד הוכיחה ביטול יצירה לאחר flush וחריגה בטרנזקציה חיצונית. אין טענה שנבדק כל כשל commit אפשרי.

## 4. על מה ויתרנו בגלל הזמן והמגבלות

- חובה ב-B1: תיקון הבאג וטסט רגרסיה. בדיקות היחידה מוכיחות את החישוב; רגרסיית PostgreSQL הורצה ועברה.
- קשור ישירות לתיקון: גבולות מכסה, שנה, קלט שגוי, אי-שמירה וחוזה JSON. נבדק בהיקף המתועד בדוח.
- החסם הסביבתי הקודם הוסר בהיקף שאומת: Docker הפעיל PostgreSQL ב-Testcontainers, וגם API עם מסד ותצוגת הממשק נבדקו בהצלחה. ניסיון ראשון נכשל באתחול Docker עם HTTP 400; הגדרת API לקוח 1.44 לתהליך הבדיקה פתרה זאת. לא שונו תלויות או הגדרות מערכת. אין לסמן את B1 כחסום בגלל התקלה ההיסטורית; אין בכך טענה שכל תרחישי B2 כבר נבדקו.
- נותר: אימות משולב של טופס F1 מול השרת והמסד לאחר תיאום, ומימוש F2–F3. טופס F1 ובדיקותיו המבודדות בוצעו. הפרדת השכבות B3 בוצעה ואומתה. אימות טרנזקציות האישור מול מסד הושלם בהיקף המתועד ב-B2_QA. בדיקת מסך לפני B2 אינה דרישה מפורשת של README.
- עם עוד זמן, לאחר דרישות F1–F3: מדיניות היסטוריית מכסות, נתונים פגומים וטווחים חופפים. השאילתה עדיין טוענת את כל החופשות המאושרות של העובד, והחישוב עובר על שנות הבקשה; אופטימיזציה להגשות חריגות בהיקפן נדחתה במסגרת ה-POC.

## 5. שימוש ב-AI

### דוגמאות אמיתיות מהשיחה (הציטוטים הם קטעים מדויקים מה-prompts)

1. Prompt: "אני רוצה שלא נפספס שום דבר אני רוצה שתכתוב לי מה הבעיה שיש בבאג מה תוקן? ומה אתה ממליץ לבדוק , אל תשנה שום דבר לפני שאתה מבקש את האישור שלי. הבדיקה וההשואה הוא ל B1"
   תוצאה: השוואה למקור, הסבר 18+3 מול 20, זיהוי פערי שנים והפרדה בין בדיקות יחידה למסד, ללא שינוי באותו שלב.
2. Prompt: "אז אני הולכת לפי ההמלצה שלך ומאשרת לך כרגע לבצע בדיקות וכן אם מצאת באג מאשרת לתקן את הקוד"
   תוצאה: הרחבת בדיקות חשפה 5 כישלונות מתוך 16; תיקון שיוך לשנה ותאריכים הפוכים, והרחבה ל-20 בדיקות שעברו. ראיות היסטוריות בדוח B1.
3. Prompt: "בבדיקה האחרונה נמצאה הרצה של 20 בדיקות יחידה שעברו, בעוד שחלק מהמסמכים עדיין מתארים שש בדיקות. אמת מה נכון כעת וסנכרן את התיעוד עם המימוש בפועל."
   תוצאה: בדיקת יומנים, הרצה חוזרת, ריכוז מטריצת QA, הוספת בדיקות MockMvc והכנת בדיקת סינון מסד ללא הפעלת Docker.

### הצעת AI שתוקנה בפועל

בסבב הביקורת המוקדם AI כתב בבדיקת JSON ציפייה ל-`"PENDING"`. ההרצה נכשלה: בפועל התקבל `0`. בדיקת `LeaveStatus.java` הראתה שהמודל מגדיר במפורש סריאליזציה מספרית, התואמת ללקוח Angular. תוקנה ציפיית הבדיקה ל-0 וגם קלט סוג החופשה הותאם לקוד המספרי הקיים. לא שינינו את חוזה ה-API כדי להתאים לבדיקה שגויה. ראיה: `.local-run/b1-audit.log`; האימות החוזר ב-`.local-run/b1-audit-final.log`. זה תיקון אמיתי של הצעת AI, לא תקלה שהתגלתה ביישום.

### המשך בפועל לאחר חזרת Docker — הרחבה לדוגמת הבדיקות

Prompt אמיתי: "התחל עכשיו בביצוע B1 לפי הפרומפט, כולל בדיקות ותיקונים, commit מקומי וללא push."

AI הרחיב את בדיקות המסד לשנים ולצבירה, הריץ 37 בדיקות, יצר נתוני בדיקה ייעודיים ובדק שמונה בקשות HTTP מול SQL ותצוגת הדפדפן. ההרצה הראשונה של Testcontainers נכשלה בתשתית; התאמת API ל-1.44 אפשרה הרצה אמיתית. תוצאות mocks לא סומנו כהוכחת מסד. הבדיקות והראיות נשמרו בקומיט המקומי `8da4450`, ללא push. לא נטען לתיקון יישום נוסף שלא נעשה. הצעת ה-AI שתוקנה בסעיף הקודם נשארת הדוגמה האמיתית לתיקון, ולא הומצאה דוגמה נוספת.

### תפקיד הבדיקות המבודדות

בדיקות Mockito ו-MockMvc נשמרות: הן בודקות לוגיקה וחוזה HTTP במהירות ומשלימות את בדיקות PostgreSQL. הן אינן מוכיחות SQL, נעילות או rollback במסד.

### אבטחה

בונוס אבטחה — תוקן ואומת בנקודת החיפוש במסגרת B3: קודם LeaveRequestsController.search שרשר שם לתוך SQL. שחזור קריאה בלבד עם `') OR TRUE --` החזיר 17 רשומות במקום 0. כעת LeaveRequestRepository.searchByEmployeeName קושר :name כפרמטר, והקלט אינו משנה את מבנה השאילתה ומחזיר רשימה ריקה. בדיקת PostgreSQL ו-HTTP הוכיחו את התיקון ואת פעולת החיפוש עבור שם עם גרש. משמעות LIKE הקיימת נשמרה, כולל wildcards; אין טענה לביקורת אבטחה מלאה. ראיה ושמות הבדיקות ב-[B3_QA](docs/B3_QA.md). אין דרישה ל-GitHub Discussions; מסמך זה הוא תוצר ההגשה הנדרש.

### המשך תיעוד AI ב-B2

התכנון שביקשה המשתמשת כלל: "אל תסתפק באמירה ‘נוסיף @Transactional’: הסבר כיצד המנגנון מגן על הכלל העסקי ב־PostgreSQL" (ציטוט עם התאמת סימני מירכאות). לאחר אישור "מאשרת" נכתב שירות אישור עם נעילת עובד וקריאת scalar לפני הנעילה, וכן בדיקות מבודדות ובדיקות מסד שהיו חסומות אז; בדיקות המסד של B2 הורצו בהמשך ועברו בהיקף המתועד בדוח. זו הרחבה לדוגמת השימוש בתכנון ובבדיקות, והאימות בפועל מתועד בנפרד בראיות B2.

בסבב האימות לאחר חזרת Docker, ה-prompt כלל: "בצע בעצמך כל בדיקה זמינה. אל תעביר אליי פעולות שאתה יכול לבצע." AI הריץ תחילה 71 בדיקות, הרחיב 12 מקרי מסד, ואז הריץ 83 בדיקות שעברו. בנוסף בוצעו 11 בקשות HTTP עם אימות SQL ואישור דרך המסך. בביקורת הקודמת תוקנה הוכחת המתנה המבוססת על timeout בלבד לחיווי נעילות מהמסד; בסבב זה הבדיקה המתוקנת הורצה בהצלחה. לא שונה קוד יישום כדי ליצור תיקון מלאכותי, ולא הומצאה דוגמת דחייה נוספת.

### המשך תיעוד AI ב-B3

Prompt אמיתי: "זהה מה כבר הופרד במהלך B2, כולל שירות האישור ומדיניות חישוב המכסה. השתמש בקיים ואל תיצור שכבות כפולות." AI הציע והוסיף שירות יצירה/קריאה אחד, שמר את שירות האישור ומדיניות המכסה, והתאים את חיבור הבדיקות בלי להחליש את assertions הקיימים. לפני השינוי עברו 83 בדיקות; לאחריו עברו 86. נוסף תיקון קשירת פרמטר לחיפוש עם הוכחת שחזור ובדיקה אחרי התיקון. אין דחייה מלאכותית חדשה; הדוגמה המתועדת של PENDING לעומת 0 נשארת הדוגמה האמיתית לתיקון הצעת AI.

### F1 — המשך לדוגמת עזרת AI בתכנון ובבדיקות

Prompt אמיתי: "עבוד רק בקובצי `frontend/` הנחוצים ל־F1 ובבדיקות שלהם." AI מימש את הטופס והבדיקות בגבולות אלה, תוך שמירת חוזה השרת והימנעות מעריכת מסמכים משותפים בזמן העבודה המקבילה על B3. לאחר אישור המשתמשת לתיאום התיעוד, סיכום F1 שולב במסמכים. זו הרחבה לדוגמת העבודה והבדיקות הקיימת, ללא דוגמת דחייה חדשה או מלאכותית.

## 6. הוראות הרצה

### פקודות F1

מתוך frontend עם Node.js המקומי שהוכן לפרויקט: `npm.cmd run build` ו-`npm.cmd test -- --watch=false --browsers=ChromeHeadless`. שתי הפקודות הורצו בהצלחה. בדיקות Chrome דרשו הרשאת הרצה מחוץ לסביבה המוגבלת לאחר כשל הפעלת הדפדפן; לא נדרשה התקנה חדשה. הוראות ההפעלה ב-README נשארו ללא שינוי. בדיקות אלה מדמות תגובות HTTP ואינן דורשות שרת פעיל.

README המקורי נשאר ללא שינוי. בסביבה המקומית קיימים JDK 21 ו-Maven. להרצה ללא Docker, משורש התיקייה ב-PowerShell:

```powershell
. .\scripts\Use-LocalTools.ps1
Set-Location backend
mvn.cmd -o '-Dmaven.repo.local=../.local-tools/m2' '-Dtest=LeaveBalanceTests,LeaveRequestHttpTests,LeaveApprovalTests,LeaveApprovalHttpTests' test
```

פקודה זו מפעילה רק בדיקות יחידה ו-MockMvc, ללא שרת רשת או מסד. היא מקמפלת גם את מקורות בדיקות PostgreSQL אך אינה מריצה אותן. במחשב אחר עם תלויות זמינות אפשר להריץ מתוך backend: `mvn -Dtest=LeaveBalanceTests,LeaveRequestHttpTests,LeaveApprovalTests,LeaveApprovalHttpTests test`.

הרצת B1 מול PostgreSQL שאומתה בסביבה המקומית (משורש המאגר):

```powershell
. .\scripts\Use-LocalTools.ps1
$env:DOCKER_HOST='npipe:////./pipe/dockerDesktopLinuxEngine'
Set-Location backend
mvn.cmd -o '-Dmaven.repo.local=../.local-tools/m2' '-Dapi.version=1.44' '-Dtest=LeaveBalanceTests,LeaveRequestHttpTests,LeaveRequestsTests' test
```

Docker צריך לפעול; Testcontainers מקים מסד בדיקה מבודד ואינו מאפס את מסד הפיתוח. `-o` מסתמך על תלויות שכבר הורדו במחשב הזה; הנתיבים והתאמת Docker מקומיים ואינם דרישה למחשב אחר. `mvn test` מריץ גם חבילות אחרות ובהן B2, ולא הורץ כסוויטה מלאה בסבב B1; בסבב B2 המאוחר הסוויטה המלאה הורצה ועברה, לפי הראיה המקושרת בסעיף 3. יומן ההרצה שעבר: `.local-run/b1-postgres-final.log`; ראיה מצומצמת ללא פרטי סביבה רגישים נשמרה במאגר.

הרצת כל בדיקות B1 ו-B2 שאומתה ב-16:55:19, לאחר אותה הכנת כלים ו-DOCKER_HOST, מתוך backend:

```powershell
mvn.cmd -o '-Dmaven.repo.local=../.local-tools/m2' '-Dapi.version=1.44' test
```

כל שש חבילות הבדיקות הורצו: 83 בדיקות ללא כישלונות, שגיאות או דילוגים. יומן: `.local-run/b2-postgres-final.log`; מקור הראיות וחתימות הקוד: [B2-postgres-run.json](docs/evidence/B2-postgres-run.json). אין צורך בהתקנה חדשה; זו התאמת הרצה מקומית, עם מסדי Testcontainers מבודדים.

### אימות והרצה לאחר B3

פקודות הבדיקה לא השתנו. אותה פקודת Maven מלאה עברה ב-2026-09-09 17:23:47 עם 86 בדיקות, אפס כישלונות/שגיאות/דילוגים. השרת נארז ב-package -DskipTests רק לאחר הצלחת הבדיקות, והופעל ב-5080 מול מסד הפיתוח הקיים ללא איפוס. ב-Windows נעילת JAR על ידי תהליך השרת הישן חייבה עצירתו המזוהה לפני אריזה מחדש; זו לא תקלה בקוד.

13 בדיקות HTTP עברו, ובקשה 21 של עובד 6 אושרה במסך ונקראה כ-APPROVED לאחר רענון, GET ו-SQL. אין שינוי חזותי או מימוש F1–F3. ראיות וחתימות המקורות: [B3-test-run.json](docs/evidence/B3-test-run.json), [B3-live-http.json](docs/evidence/B3-live-http.json). התיעוד והיישום מיועדים לקומיט המקומי המורשה בענף codex/b3-layers; לא בוצע push בסבב זה.
