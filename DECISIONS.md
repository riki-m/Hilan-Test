# DECISIONS — מצב B1

## 1. החלטות ארכיטקטוניות

תיקון ממוקד ב-`backend/src/main/java/com/example/leavemanagement/controller/LeaveRequestsController.java`, במתודה `create` ובפונקציית עזר פרטית `daysWithin`. ללא תלויות חדשות או שינוי מבנה. הפרדת שכבות שייכת ל-B3 ולא בוצעה. בדיקות יחידה משתמשות ב-Mockito; בדיקות MockMvc בודקות מיפוי ו-JSON בתוך התהליך, ללא שרת רשת. בדיקות PostgreSQL הקיימות נשמרו והורחבו, אך לא הורצו בסבב הנוכחי.

## 2. הבאג ביתרת החופשה

במקור (`455571f`) חושב הניצול המאושר אך התנאי בדק רק את הבקשה החדשה מול המכסה המלאה. לדוגמה: מכסה 20, ניצול 18, בקשה 3 — התקבלה בטעות. כעת נבדק סכום הניצול המאושר והימים המבוקשים בכל שנה מול המכסה. חריגה מחזירה 400 לפני שמירה; בדיוק היתרה מותרת ונשמרת במצב PENDING.

מדיניות עסקית מפורשת: שנה קלנדרית, ללא העברת יתרות; בקשה חוצת שנה מחולקת לפי חפיפת התאריכים לכל שנה. זו הנחת מימוש שלנו, משום שהמטלה אומרת מכסה שנתית אך אינה מגדירה את השיוך. המכסה הנוכחית של העובד חלה בכל שנה; המודל אינו מכיל היסטוריית שינויי מכסה. ימים קלנדריים כוללים את שני קצות הטווח, בהתאם להערה במודל. הסכימה משתמשת ב-long, ומספר הימים הנשמר חייב להתאים ל-int.

רק VACATION במצב APPROVED של העובד נחשב ניצול. PENDING ו-REJECTED אינם שומרים מכסה. מחלה וחל״ת אינן מוגבלות במכסת חופשה. חופשות היסטוריות מחושבות מהתאריכים, ולא משדה days שעלול להיות לא עקבי. לא מבוצע תיקון אוטומטי של היסטוריה פגומה או איחוד טווחים חופפים.

נוספה בדיקת שדות חובה וסדר תאריכים בשרת כדי למנוע שמירת בקשות לא תקינות ועקיפת מכסה באמצעות ימים שליליים. אין שינוי ב-Angular.

ראיות עדכניות ותרחישים מרוכזים רק ב-[דוח B1](docs/B1_QA.md): 20 בדיקות יחידה ובדיקות MockMvc נפרדות. ההפניות הישנות לשש בדיקות מתארות שלב היסטורי בלבד. בדיקת הרגרסיה `create_ExceedingRemainingQuota_IsRejectedWithoutSaving` מול PostgreSQL קיימת בקוד אך חסומה להרצה; אין להציג אותה כעברה.

## 3. אישור בקשה (approve) ו-concurrency

B2 לא התחיל. אין כאן הגנה על אישורים במקביל או שמירת מכסה לבקשות ממתינות. אין להסיק מבדיקת יצירה שאישור עתידי בטוח.

## 4. על מה ויתרנו בגלל הזמן והמגבלות

- חובה ב-B1: תיקון הבאג וטסט רגרסיה. בדיקות היחידה מוכיחות את החישוב; רגרסיית PostgreSQL ממתינה לאימות.
- קשור ישירות לתיקון: גבולות מכסה, שנה, קלט שגוי, אי-שמירה וחוזה JSON. נבדק בהיקף המתועד בדוח.
- חסום בהנחיית המשתמשת: הפעלת Docker/שרת, PostgreSQL, HTTP ברשת ותהליך במסך. אין הכרזה על סגירת B1 מקצה לקצה.
- נדחה: טרנזקציות אישור ל-B2, הפרדת שכבות ל-B3, תיקוני מסך ל-F1–F3. בדיקת מסך לפני B2 אינה דרישה מפורשת של README.
- עם עוד זמן: אימות מסד וממשק תחילה; אחריו מדיניות היסטוריית מכסות, נתונים פגומים וטווחים חופפים. השאילתה עדיין טוענת את כל החופשות המאושרות של העובד, והחישוב עובר על שנות הבקשה; אופטימיזציה להגשות חריגות בהיקפן נדחתה במסגרת ה-POC.

## 5. שימוש ב-AI

### דוגמאות אמיתיות מהשיחה (הציטוטים הם קטעים מדויקים מה-prompts)

1. Prompt: "אני רוצה שלא נפספס שום דבר אני רוצה שתכתוב לי מה הבעיה שיש בבאג מה תוקן? ומה אתה ממליץ לבדוק , אל תשנה שום דבר לפני שאתה מבקש את האישור שלי. הבדיקה וההשואה הוא ל B1"
   תוצאה: השוואה למקור, הסבר 18+3 מול 20, זיהוי פערי שנים והפרדה בין בדיקות יחידה למסד, ללא שינוי באותו שלב.
2. Prompt: "אז אני הולכת לפי ההמלצה שלך ומאשרת לך כרגע לבצע בדיקות וכן אם מצאת באג מאשרת לתקן את הקוד"
   תוצאה: הרחבת בדיקות חשפה 5 כישלונות מתוך 16; תיקון שיוך לשנה ותאריכים הפוכים, והרחבה ל-20 בדיקות שעברו. ראיות היסטוריות בדוח B1.
3. Prompt: "בבדיקה האחרונה נמצאה הרצה של 20 בדיקות יחידה שעברו, בעוד שחלק מהמסמכים עדיין מתארים שש בדיקות. אמת מה נכון כעת וסנכרן את התיעוד עם המימוש בפועל."
   תוצאה: בדיקת יומנים, הרצה חוזרת, ריכוז מטריצת QA, הוספת בדיקות MockMvc והכנת בדיקת סינון מסד ללא הפעלת Docker.

### הצעת AI שתוקנה בפועל

בסבב הנוכחי AI כתב בבדיקת JSON ציפייה ל-`"PENDING"`. ההרצה נכשלה: בפועל התקבל `0`. בדיקת `LeaveStatus.java` הראתה שהמודל מגדיר במפורש סריאליזציה מספרית, התואמת ללקוח Angular. תוקנה ציפיית הבדיקה ל-0 וגם קלט סוג החופשה הותאם לקוד המספרי הקיים. לא שינינו את חוזה ה-API כדי להתאים לבדיקה שגויה. ראיה: `.local-run/b1-audit.log`; האימות החוזר ב-`.local-run/b1-audit-final.log`. זה תיקון אמיתי של הצעת AI, לא תקלה שהתגלתה ביישום.

### אבטחה

ב-`LeaveRequestsController.search` נבנה SQL בשרשור קלט שם. הסיכון הוא SQL injection; הטיפול נשאר לבונוס S1 ולא בוצע כחלק מ-B1. אין טענה שהפרויקט עבר ביקורת אבטחה מלאה. אין צורך ב-GitHub Discussions: מסמך זה הוא תוצר ההגשה הנדרש.

## 6. הוראות הרצה

README המקורי נשאר ללא שינוי. בסביבה המקומית קיימים JDK 21 ו-Maven. להרצה ללא Docker, משורש התיקייה ב-PowerShell:

```powershell
. .\scripts\Use-LocalTools.ps1
Set-Location backend
mvn.cmd -o '-Dmaven.repo.local=../.local-tools/m2' '-Dtest=LeaveBalanceTests,LeaveRequestHttpTests' test
```

פקודה זו מפעילה רק בדיקות יחידה ו-MockMvc, ללא שרת רשת או מסד. היא מקמפלת גם את מקורות בדיקות PostgreSQL אך אינה מריצה אותן. במחשב אחר עם תלויות זמינות אפשר להריץ מתוך backend: `mvn -Dtest=LeaveBalanceTests,LeaveRequestHttpTests test`.

רק לאחר שינוי ההנחיה והכנת סביבה: `mvn test` מתוך backend מפעיל גם את Testcontainers ודורש Docker פעיל. פקודה זו לא הורצה בסבב הנוכחי. ההגדרות המקומיות אינן מחייבות מחשב של בודק אחר.
